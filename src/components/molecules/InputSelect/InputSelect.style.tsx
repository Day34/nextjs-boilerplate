import styled from 'styled-components';
import OriginSelect from '@material-ui/core/Select';
import OriginMenuItem from '@material-ui/core/MenuItem';

export const Select = styled(OriginSelect)``;
export const Option = styled(OriginMenuItem)``;
